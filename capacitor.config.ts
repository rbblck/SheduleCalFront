import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atlantatools.angular',
  appName: 'atlantatools',
  webDir: 'dist/shedule-cal-front'
};

export default config;
